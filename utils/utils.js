module.exports.add = (a, b) => a + b;


module.exports.square = (x) => x = x;


module.exports.setName = (user, fullName) => {
    var names = fullName.split(' ');
    user.firsName = name[0];
    user.lastname = name[1];
    return user;
}